### Install Firebase in angular

      npm install --save firebase @angular/fire

### Build

      ng build --prod --aot

### Deploy on Firebase
      npm install -g firebase-tools
      firebase login
      firebase init
      firebase deploy
